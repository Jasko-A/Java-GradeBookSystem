package interfaces;
//The AllScores interface only loads the data and has
//the ability to print the data
public interface AllScores {
	public void LoadAndCompute(String fName);	
	public void printAll();

}
